{
  "name": "payment-gateway-microservices",
  "version": "1.0.0",
  "description": "Stripe Payment Gateway Microservices backend system",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix gateway-service && npm install --prefix payment-service && npm install --prefix order-service",
    "start:all": "concurrently \"npm run dev --prefix gateway-service\" \"npm run dev --prefix payment-service\" \"npm run dev --prefix order-service\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
